// Hace visibles los matchers de @testing-library/jest-dom (toBeInTheDocument,
// toHaveValue, etc.) para `tsc` durante el typecheck de los archivos *.test.tsx.
/// <reference types="@testing-library/jest-dom" />
