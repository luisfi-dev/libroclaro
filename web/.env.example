# Variables para `npm run dev` (dev server de Vite).
# Vite también carga .env.production al hacer `npm run build` (y dentro de Docker).
VITE_API_URL=http://localhost:4000
VITE_API_PROXY=http://localhost:4000
